type Movie {
    _id: ID!
    title: String
    released: Int
    tagline: String
}

type Person {
  _id: ID!
  name: String
  born: Int
}

type SearchHistory @isAuthenticated {
  query: String
  date: String
}